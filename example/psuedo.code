package main

import (
	"fmt"
	"github.com/henderjon/shutdown"
)

func main() {

	signal := make(shutdown.SignalChan)

	go shutdown.Watch(signal, func() {
		fmt.Printf("hutting down in 3...2...1...")
	})

	for {
		select {
		case <-signal:
			// we're shutting down, do your cleaup here
		default:
			// do your work here
		}
	}


}
